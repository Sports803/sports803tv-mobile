import { Alert, FlatList, Pressable, Text, View } from "react-native";
import { useRouter } from "expo-router";
import { useCallback, useEffect, useMemo, useState } from "react";
import { ScreenContainer } from "@/components/screen-container";
import { clearLocalData, getDataSaver, getLeagueFavorites, getTeamFavorites, setDataSaver, toggleLeagueFavorite, toggleTeamFavorite } from "@/lib/local";
import { fetchEvents, type SportsEvent } from "@/lib/sports";
import { requestNotificationPermission } from "@/lib/notifications";

const card = { backgroundColor: "#11182A", borderColor: "#26314A", borderWidth: 1, borderRadius: 18, padding: 16, marginTop: 12 } as const;

export default function MoreScreen() {
  const router = useRouter();
  const [events, setEvents] = useState<SportsEvent[]>([]);
  const [teams, setTeams] = useState<string[]>([]);
  const [leagues, setLeagues] = useState<string[]>([]);
  const [dataSaver, setDataSaverState] = useState(false);
  const load = useCallback(async () => { const [items, teamPrefs, leaguePrefs] = await Promise.all([fetchEvents(), getTeamFavorites(), getLeagueFavorites()]); setEvents(items); setTeams(teamPrefs); setLeagues(leaguePrefs); }, []);
  useEffect(() => { void load(); void getDataSaver().then(setDataSaverState); }, [load]);
  const teamOptions = useMemo(() => Array.from(new Set(events.flatMap((event) => [event.homeName, event.awayName].filter(Boolean) as string[]))).slice(0, 12), [events]);
  const leagueOptions = useMemo(() => Array.from(new Set(events.map((event) => event.leagueName || event.competitionLabel).filter(Boolean) as string[])).slice(0, 12), [events]);
  return <ScreenContainer containerClassName="bg-background" className="px-4"><FlatList data={["content"]} renderItem={() => null} keyExtractor={(item) => item} ListHeaderComponent={<View style={{ paddingTop: 14, paddingBottom: 28 }}><Text style={{ color: "#E0102A", fontSize: 12, fontWeight: "900", letterSpacing: 1.5 }}>SPORTS 803</Text><Text style={{ color: "#F7F8FC", fontSize: 28, fontWeight: "900", marginTop: 4 }}>More</Text><Text style={{ color: "#9AA6BE", marginTop: 8, lineHeight: 20 }}>Personalize your schedule, reminders, and viewing experience.</Text><View style={card}><Text style={{ color: "#F7F8FC", fontSize: 17, fontWeight: "800" }}>Notifications</Text><Text style={{ color: "#9AA6BE", marginTop: 8, lineHeight: 19 }}>Allow local reminders 15 minutes before your selected matches begin.</Text><Pressable onPress={async () => Alert.alert("Notifications", (await requestNotificationPermission()) ? "Notifications are enabled." : "Notifications were not enabled on this device.")} style={{ marginTop: 14 }}><Text style={{ color: "#36D399", fontWeight: "900" }}>Enable match alerts</Text></Pressable></View><View style={card}><Text style={{ color: "#F7F8FC", fontSize: 17, fontWeight: "800" }}>Favorite teams</Text><Text style={{ color: "#9AA6BE", marginTop: 6 }}>Your Home screen can prioritize these teams.</Text><View style={{ flexDirection: "row", flexWrap: "wrap", gap: 8, marginTop: 12 }}>{teamOptions.map((name) => <Pressable key={name} onPress={async () => setTeams(await toggleTeamFavorite(name))} style={{ backgroundColor: teams.includes(name) ? "#E0102A" : "#17213A", borderRadius: 16, paddingHorizontal: 10, paddingVertical: 8 }}><Text style={{ color: "#F7F8FC", fontSize: 11, fontWeight: "800" }}>{teams.includes(name) ? "★ " : "☆ "}{name}</Text></Pressable>)}</View></View><View style={card}><Text style={{ color: "#F7F8FC", fontSize: 17, fontWeight: "800" }}>Favorite leagues</Text><Text style={{ color: "#9AA6BE", marginTop: 6 }}>Follow competitions that matter to you.</Text><View style={{ flexDirection: "row", flexWrap: "wrap", gap: 8, marginTop: 12 }}>{leagueOptions.map((name) => <Pressable key={name} onPress={async () => setLeagues(await toggleLeagueFavorite(name))} style={{ backgroundColor: leagues.includes(name) ? "#E0102A" : "#17213A", borderRadius: 16, paddingHorizontal: 10, paddingVertical: 8 }}><Text style={{ color: "#F7F8FC", fontSize: 11, fontWeight: "800" }}>{leagues.includes(name) ? "★ " : "☆ "}{name}</Text></Pressable>)}</View></View><View style={card}><Text style={{ color: "#F7F8FC", fontSize: 17, fontWeight: "800" }}>Playback & data</Text><Text style={{ color: "#9AA6BE", marginTop: 6 }}>Reduce refresh frequency and image loading on mobile data.</Text><Pressable onPress={async () => setDataSaverState(await setDataSaver(!dataSaver))} style={{ marginTop: 14 }}><Text style={{ color: dataSaver ? "#36D399" : "#9AA6BE", fontWeight: "900" }}>{dataSaver ? "✓ Data saver enabled" : "Enable data saver"}</Text></Pressable><Text style={{ color: "#9AA6BE", fontSize: 12, marginTop: 12 }}>Picture-in-picture is available on compatible native video playback. Casting controls are prepared for future device integration.</Text></View><View style={card}><Text style={{ color: "#F7F8FC", fontSize: 17, fontWeight: "800" }}>Explore</Text><Pressable onPress={() => router.push("/results" as any)} style={{ marginTop: 14 }}><Text style={{ color: "#36D399", fontWeight: "900" }}>Open results archive</Text></Pressable><Pressable onPress={() => router.push("/news" as any)} style={{ marginTop: 12 }}><Text style={{ color: "#36D399", fontWeight: "900" }}>Open news & highlights</Text></Pressable><Pressable onPress={() => router.push({ pathname: "/explore" as any, params: { kind: "league", value: "Premier League" } })} style={{ marginTop: 12 }}><Text style={{ color: "#36D399", fontWeight: "900" }}>Browse league pages</Text></Pressable><Text style={{ color: "#9AA6BE", fontSize: 12, marginTop: 5 }}>Finished matches are kept here instead of cluttering the live schedule.</Text></View><View style={card}><Text style={{ color: "#F7F8FC", fontSize: 17, fontWeight: "800" }}>Local preferences</Text><Text style={{ color: "#9AA6BE", marginTop: 8 }}>Favorites, reminders, reports, and recently viewed events stay on this device.</Text><Pressable onPress={() => Alert.alert("Clear local data", "Remove favorites, reminders, reports, and recently viewed events?", [{ text: "Cancel", style: "cancel" }, { text: "Clear", style: "destructive", onPress: () => void clearLocalData().then(load) }])} style={{ marginTop: 16 }}><Text style={{ color: "#E0102A", fontWeight: "800" }}>Clear local data</Text></Pressable></View><Text style={{ color: "#66718A", fontSize: 11, marginTop: 22 }}>Version 1.0.0 • Free sports streaming companion</Text></View>} /></ScreenContainer>;
}
